
modules.exports = function coreosCluster(manifest) {
  // return { machineGenerator };

  // function* machineGenerator() {
  //   _.each(manifest.configuration, (roleName, count) => {
  //     repeat(count, () => yield createMachine(role));
  //   });
  // }

  // function createMachine(roleName) {
  //   var role = manifest.roles[roleName];

  //   // roleName: [container, container]
  //   var units = _.map(role, createUnit);
  // }

  // function createUnit(containerManifest, name) {

  // }

  // function repeat(times, fn) {
  //   for (var i = 0; i < times; i++) fn();
  // }
};